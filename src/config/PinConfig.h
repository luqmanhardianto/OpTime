#pragma once

#include <stdint.h>

#include "bsp/BoardConfig.h"

constexpr uint8_t PIN_RTC_SQW = Board::Pin::RTC_SQW;
constexpr uint8_t PIN_BUZZER = Board::Pin::BUZZER;
constexpr uint8_t PIN_BUTTON_POWER = Board::Pin::BUTTON_POWER;
constexpr uint8_t PIN_BUTTON_SELECT = Board::Pin::BUTTON_SELECT;
constexpr uint8_t PIN_BUTTON_NEXT = Board::Pin::BUTTON_NEXT;
constexpr uint8_t PIN_BUTTON_UP = Board::Pin::BUTTON_UP;
constexpr uint8_t PIN_BUTTON_DOWN = Board::Pin::BUTTON_DOWN;
constexpr uint8_t PIN_DISPLAY_OE = Board::Pin::DISPLAY_OE;
constexpr uint8_t PIN_SHIFT_LATCH = Board::Pin::DISPLAY_LATCH;
constexpr uint8_t PIN_SHIFT_DATA = Board::Pin::DISPLAY_DATA;
constexpr uint8_t PIN_POWER_LED = Board::Pin::POWER_LED;
constexpr uint8_t PIN_SHIFT_CLOCK = Board::Pin::DISPLAY_CLOCK;
